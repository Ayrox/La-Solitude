import { EmbedBuilder } from 'discord.js'
import config from "../../config.js"


export const event = {
    name: 'stickerUpdate',
    once: false,

    execute(oldSticker, newSticker) {

       
    }
}
