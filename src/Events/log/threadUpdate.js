import { EmbedBuilder } from 'discord.js'
import config from "../../config.js"



export const event = {
    name: 'threadUpdate',
    once: false,

    execute(oldThread, newThread) {

       
    }
}